# SQL Server Configuration
# Copy this file to .env and adjust as needed

# Project version (from version.json)
# Used for Docker image tagging - run ./set-version.sh to auto-populate
VERSION=1.0.0

# SQL Server 2022 - Works on both Apple Silicon (via Rosetta 2) and Intel/AMD
SQL_IMAGE=mcr.microsoft.com/mssql/server:2022-latest

# Note: Azure SQL Edge has known issues on Apple Silicon:
# - Latest tag has database path bugs
# - Version 1.0.7 crashes with ARM64 instruction errors
# Therefore, SQL Server 2022 with Rosetta 2 is recommended

# Database password (must be strong: 8+ chars, uppercase, lowercase, numbers, symbols)
SQL_SA_PASSWORD=YourStrong@Passw0rd

# Port mapping
SQL_PORT=1433

# API Service Configuration
# Which data API should nextui connect to?
# Options: http://fastDataApi:8000 or http://flaskDataApi:8001
API_SERVICE_URL=http://fastDataApi:8000
